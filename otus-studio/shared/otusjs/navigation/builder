/* Rota direta, sem condição a ser respeitada */
builder
    .createRoute()
    .from('CID')
    .to('NID');

/* Rota que ocorre sob determinada condição */
builder
    .createRoute()
    .from('CID')
    .to('NID')
    .underCondition
    .question('QID')
    .answer
    .isEqualTo(5)
    .or
    .isGreaterThan(10)
    .and
    .question('BQID')
    .hasNotAnswer();
